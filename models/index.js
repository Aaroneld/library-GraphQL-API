const books = require("./books");
const authors = require("./authors");
const libraries = require("./libraries");
const awards = require("./awards");

module.exports = { books, authors, libraries, awards };
